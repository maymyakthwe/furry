working on
//user page
//user ko props htae
//click tk link pw mu t pee prop ko pyg
//edit profile
    
front// '/' // 'service' // 'adoption' // 'adoption pet'// 'adoption form' //***
admin//'petcreate'/**
back// 
    pet - /upload - (pet-img-upload)
            - /admin/pets - (create pets) 
            - /pets - (read all pets) 
            - /pets/:petId (read one pet)
    
    user    -/signup (create user/return jwt)
            -/login (read one user/compare /return jwt)
            -/user/:userId (read one user/return user info)

    adoptionform - /adoptionForm (create adp form)


notes***
//revisit schemas
//.populate
//jwt sign/verify
//edit profile / front end
//user Update / back end

//change state value from user(frontend) according to the link clicked from navbar profile links
//example - click adoption - make adoption already selected in User page


//change name User to User page (optional) **